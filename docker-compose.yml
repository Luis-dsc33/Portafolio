# docker-compose.yml
version: '3.8'

services:
  frontend:
    build: 
      context: ./frontend
    container_name: retro-frontend
    ports:
      - "8080:80" # Mapeamos el puerto 80 del contenedor al 8080 de tu máquina
    restart: unless-stopped